# The toolchain is stdlib-only except for the map editor's UI.
# Everything else -- archive.py, resource_format.py, mdl16_format.py, modmanager.py,
# zax_render.py, mapedit_core.py -- runs with no third-party packages at all.
PySide6>=6.5      # mapedit.py only
